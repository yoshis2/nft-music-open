export type BusinessMasterItem = {
  id: string;
  name: string;
  created_at: string;
  updated_at: string; // todo maybe in smart contract also?
};

export type GenreMasterItem = {
  id: string;
  name: string;
  created_at: string;
  updated_at: string; // todo maybe in smart contract also?
};
